{
  "name": "better-with-discord-premium",
  "version": "0.2.0",
  "private": true,
  "description": "Mobile plugin package for Better with Discord Premium",
  "license": "MIT",
  "scripts": {
    "test": "node tests/parser.test.js",
    "check": "node --check plugin/index.js && node --check plugin/src/parser.js",
    "prepare:manifest": "node scripts/prepare-manifest.mjs",
    "pack": "bash scripts/pack.sh",
    "build": "npm run check && npm run test && npm run pack"
  }
}
